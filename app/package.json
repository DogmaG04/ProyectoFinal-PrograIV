{
  "name": "surtidor-gasolina",
  "private": true,
  "scripts": {
    "dev": "concurrently --names BE,FE --prefix-colors cyan,yellow \"cd backend && pnpm dev\" \"cd frontend && pnpm dev\"",
    "build": "cd frontend && pnpm install && pnpm build",
    "start": "cd backend && pnpm install && pnpm start",
    "install:all": "cd backend && pnpm install && cd ../frontend && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
